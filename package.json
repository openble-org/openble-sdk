{
  "name": "@openble/openble-sdk",
  "version": "0.1.1",
  "description": "OpenBLE SDK",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "tiny-invariant": "^1.3.1"
  }
}
